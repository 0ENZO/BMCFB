<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top d-print-none">
    <a class="navbar-brand font_logo" href="{{ path('home') }}">BMCFB</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">

        <ul class="navbar-nav ml-auto">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <div class="dropdown-divider d-md-none"></div>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    {{ app.user.email }}
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('user_profile') }}"><i class="fas fa-user-circle fa-fw mr-2"></i>Mon compte</a>
                    {% if is_granted('ROLE_ADMIN') %}
                    <a class="dropdown-item" href="{{ path('admin') }}">
                        <i class="fas fa-tools fa-fw mr-2"></i>Administration
                    </a>
                    {% endif %}
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('app_logout') }}"><i class="fas fa-sign-out-alt fa-fw mr-2"></i>Se
                        déconnecter</a>
                </div>
            </li>
            {% else %}
            <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Connexion</a></li>
            {% endif %}

        </ul>
    </div>
</nav>